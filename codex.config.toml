# Global defaults for Codex CLI when working on MedBattle
[defaults]
  model = "gpt-5-codex"
  model_reasoning_effort = "high"
  windows_wsl_setup_acknowledged = true
  approval_policy = "Full access"
  hide_full_access_warning = true

# Project specific overrides to ensure approval is always level 3
[projects.'C:\Users\simon\Apps\MedBattle']
  trust_level = "trusted"
  approval_policy = "Full access"
  sandbox_mode = "danger-full-access"
